# Generated by web/scripts/gen-demo-abis.mjs (ADR-0016). Its own JSON.stringify output is the
# canonical form the generator's --check compares against byte-for-byte; Prettier's quote-props
# rewrite would make every `--check` run report drift after any `prettier --write` pass that
# touches web/src/demo/.
web/src/demo/abis.generated.ts
