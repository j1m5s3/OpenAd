{
  "name": "@openad/e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Playwright journeys driven by YAML scenarios (ADR-0010).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install chromium",
    "test:demo": "playwright test -c demo/demo.config.ts",
    "capture:screenshots": "node demo/capture-screenshots.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "js-yaml": "^4.1.0",
    "@types/js-yaml": "^4.0.9"
  }
}
