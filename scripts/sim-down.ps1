$ErrorActionPreference = 'Stop'

Write-Host "The sim daemon is a titled cmd window (openad-sim)."
Write-Host "Stop it by closing that window or pressing Ctrl+C inside it."
Write-Host "This script does not kill processes (ADR-0007)."
