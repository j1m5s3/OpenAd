# Serve CDN worker (ROADMAP 4.4)

Source-only Cloudflare Worker that can cache `GET /v1/serve/{slot}` JSON and media.
**Not deployed.** The API process remains the origin.

```text
npx wrangler dev   # optional local
# do not wrangler deploy unless ops asks
```
